import { Project } from './project';

export class SpecialUser {
    user:{
        id: number;
        username: String;
        password: String;
        //project: number;
        //projects: Project[];
    }
}